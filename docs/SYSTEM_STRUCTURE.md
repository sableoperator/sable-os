# SableOS System Structure

SableOS has two separate working areas: the real local workstation environment and the clean GitHub repository.

Keeping these separate prevents private files, logs, backups, and machine-specific configuration from being accidentally published.

## Real Workstation Environment

The real working system currently lives at:

```text
~/JasonOS
